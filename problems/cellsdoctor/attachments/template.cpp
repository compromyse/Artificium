#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int iteration;
    cin >> iteration; cin.ignore();
    int size;
    cin >> size; cin.ignore();
    for (int i = 0; i < size; i++) {
        string row;
        getline(cin, row);
    }
}
